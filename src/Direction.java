package UNO;
/**
 * A Direction enum has two possible values: CLOCK_WISE, COUNTER_CLOCKWISE
 * Represents the direction of turns (of players)
 */
public enum Direction
{
    CLOCKWISE, COUNTER_CLOCKWISE;
}